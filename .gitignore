# NEVER commit credentials
config.yaml

# Claude Code project settings
.claude/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Ansible temp files
*.retry
/tmp/deploy_inv_*

# Editor
.vscode/
*.swp
*.bak

# Test plans (ephemeral — deleted as tests pass)
test-plan.md

# Log files / log directory
logs/
**/*.log
*.log

# Deployment records (real deployments — examples are tracked explicitly)
deployments/lxc/*.json
deployments/vms/*.json
deployments/bigip/*.json
!deployments/lxc/example-*.json
!deployments/vms/example-*.json
!deployments/bigip/example-*.json

# Vendor appliance images (large, licensed — never commit)
# Ignore everything in the folder EXCEPT the README that documents how to use it.
appliance-images/*
!appliance-images/README.md
CLAUDE.md

# Claude Code
CLAUDE.md
.claude/
