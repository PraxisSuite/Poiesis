---
# ============================================================
# Debian / Ubuntu OS-specific variables
# Loaded automatically by post-deploy*.yml via:
#   include_vars: "vars/{{ ansible_os_family }}.yml"
# ============================================================

ssh_service: ssh
sudo_group: sudo
chrony_conf_path: /etc/chrony/chrony.conf
chrony_service: chrony

packages:
  # --- Network tools ---
  - net-tools          # ifconfig, netstat, route, arp
  - iproute2           # ip addr, ip route, ss, tc
  - nmap               # port scanner
  - iputils-ping       # ping, arping
  - traceroute
  - mtr                # combined ping+traceroute
  - dnsutils           # dig, nslookup, host
  - netcat-openbsd     # nc - network utility
  - socat              # advanced netcat
  - tcpdump            # packet capture
  - iperf3             # network bandwidth testing
  - fping              # fast parallel ping
  - arp-scan           # ARP scanner
  - ethtool            # ethernet settings
  - iftop              # live bandwidth monitor
  - nload              # network load monitor
  - curl               # HTTP client
  - wget               # HTTP/FTP download

  # --- Text editors ---
  - nano
  - vim

  # --- Data/JSON/YAML tools ---
  - jq                 # JSON processor
  - yamllint           # YAML linter
  - python3-jsonschema # JSON schema validation

  # --- Shell/script linting ---
  - shellcheck

  # --- Archive/compression tools ---
  - zip
  - unzip
  - tar
  - gzip
  - bzip2
  - xz-utils
  - p7zip-full         # 7z

  # --- SSH tools ---
  - sshpass            # non-interactive SSH password auth
  - openssh-client

  # --- NTP client ---
  - chrony

  # --- NFS client ---
  - nfs-common
  - nfs4-acl-tools

  # --- SNMP ---
  - snmpd
  - snmp               # snmpwalk, snmpget, etc.
  - snmp-mibs-downloader

  # --- Hardware inventory tools ---
  - lshw
  - pciutils           # lspci
  - usbutils           # lsusb
  - dmidecode          # DMI/SMBIOS hardware info
  - hdparm
  - smartmontools      # S.M.A.R.T. disk monitoring
  - hwinfo

  # --- System monitoring & process tools ---
  - htop
  - iotop
  - lsof
  - strace
  - sysstat            # sar, iostat, mpstat
  - dstat

  # --- Development / misc ---
  - git
  - rsync
  - screen
  - tmux
  - tree
  - less
  - psmisc             # killall, fuser, pstree
  - procps             # ps, top, vmstat, free, etc.
  - util-linux

  # --- Python ---
  - python3
  - python3-pip
  - python3-venv

  # --- Misc useful ---
  - bc
  - at
  - cron
  - logrotate
